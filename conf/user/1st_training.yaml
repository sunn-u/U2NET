model:
  weight:
  device:
  train: True
dataset:
  train:
  test:
dataloader:
  augmentation:
    use: True
    save:
    multiple:
    func:
      HorizontalFlipping: True
training:
  epochs: 100
  ims_per_batch: 32
  checkpoint_period: 1
  output_dir: